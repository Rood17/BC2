<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mobileapp.bouldercorpregistration">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_brand_black"
        android:label="BC Registration"
        android:roundIcon="@mipmap/bc_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BoulderCorpRegistration.NoTitlle"
        tools:replace="android:label">

        <activity
            android:name=".Welcome"
            android:theme="@style/Theme.BoulderCorpRegistration.NoTitlle.NoActionBar"></activity>
        <activity android:name=".Final" />
        <activity android:name=".ReviewDoc" />
        <activity android:name=".DataNotice" />
        <activity android:name=".RegistrationForm" />
        <activity android:name=".MainActivity">

            <!-- android:screenOrientation="landscape" -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>